const {
  Connection,
  PublicKey,
  Transaction,
  TransactionInstruction,
  SystemProgram,
} = require('@solana/web3.js');

window.SolanaBundle = {
  Connection,
  PublicKey,
  Transaction,
  TransactionInstruction,
  SystemProgram,
};
